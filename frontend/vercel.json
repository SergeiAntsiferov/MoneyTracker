{
    "redirects":[
        { 
            "source" :"/api/:match*",
            "destination":"https://transactions-backend.vercel.app/api/:match*"
        }
    ],
    "rewrites": [
    { 
        "source" :"/api/:match*",
        "destination":"https://transactions-backend.vercel.app/api/:match*"
    }
  ]
}